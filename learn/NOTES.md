# Notes

- 2026-08-14: First session. Asked for the overall sprint-ledger fetch flow and the roles of `SPRINT_BOARD_META_QUERY_KEY` and `SPRINT_BOARD_QUERY_KEY`. Currently focused on `lib/query/sprint-board.ts`.
- Mission inferred from that request + this being their own app. Revise `MISSION.md` if the real goal is something else (e.g. they want to delete the `"active"` key, or add a second consumer).
